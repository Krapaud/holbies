document.addEventListener('DOMContentLoaded', () => {    const pythonCodeTextarea = document.getElementById('python-code');    const runCodeButton = document.getElementById('run-code');    const codeOutputPre = document.getElementById('code-output');    runCodeButton.addEventListener('click', async () => {        const code = pythonCodeTextarea.value;        codeOutputPre.textContent = 'Executing code...';        try {            const response = await fetch('/run-python-code', {                method: 'POST',                headers: {                    'Content-Type': 'application/json',                },                body: JSON.stringify({ code: code }),            });            const data = await response.json();            if (response.ok) {                codeOutputPre.textContent = data.output;            } else {                codeOutputPre.textContent = `Error: ${data.error || 'Unknown error'}\n${data.output || ''}`;            }        } catch (error) {            codeOutputPre.textContent = `Network error: ${error.message}`;        }    });});